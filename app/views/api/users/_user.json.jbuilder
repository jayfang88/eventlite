json.extract! user, :id, :email, :fname, :lname

#json.events here's the field for events and use user's association and then shape into arrays
#some json for users's attending events as an array
#some json for user's bookmarked events as an array